# Development & test dependencies (not installed in production Docker image)
# Install with: pip install -r requirements-dev.txt

# Testing
pytest==8.4.0
pytest-asyncio==0.26.0
httpx==0.28.1

# Linting
flake8==7.3.0
